const FROM_EMAIL = "no-reply@snpclic.fr";
const FROM_NAME = "SNPClic";
const FROM = FROM_NAME + " <" + FROM_EMAIL + ">";

export const emailConfig = {
  from: FROM,
  fromEmail: FROM_EMAIL,
  fromName: FROM_NAME,
}
